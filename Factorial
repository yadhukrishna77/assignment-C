#include<stdio.h>
int factorial(int n)
  {
      int i,fact=1;
      for(i=1;i<=n;i++)
      {
        fact*=i;
      }
      return fact;
  }
void main()
  {
      int n;
      printf("Enter a number : ");
      scanf("%d",&n);
      printf("\nThe factorial of %d = %d",n,factorial(n));
  }
